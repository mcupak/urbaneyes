Server portion of the UrbanEyes application containing the management interface.

Java EE 6 application, Maven project.

To run, build the application via "mvn clean package" and copy the war archive from the target directory to the deployment directory of your server. Tested with JBoss AS 7.1.1.Final (http://www.jboss.org/jbossas/).